---
import Sidebar from "../components/Sidebar.astro";
import TopNav from "../components/TopNav.astro";
const { title = "Portfolio" } = Astro.props;
---

<!doctype html>
<html lang="en" class="bg-zinc-950 text-zinc-100">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
  </head>
  <body class="min-h-screen">
    <div class="mx-auto max-w-6xl px-4 py-6">
      <TopNav />
      <div class="mt-6 grid grid-cols-1 gap-6 md:grid-cols-[280px_1fr]">
        <aside class="md:sticky md:top-6 md:h-[calc(100vh-6rem)]">
          <Sidebar />
        </aside>
        <main class="rounded-2xl border border-zinc-800 bg-zinc-900/40 p-5 md:p-8">
          <slot />
        </main>
      </div>
      <footer class="mt-10 pb-8 text-center text-xs text-zinc-500">
        Built with Astro + GitHub Pages • Editable at /admin
      </footer>
    </div>
  </body>
</html>